# 2026-05-15

- Verified the live deployed compare path is currently using deterministic `gladysInsight`, not `gladysV2Insight`.
- Confirmed the UI is v2-ready but the deployed `api/compare.js` route still returns `gladysV2Insight: null`.
- Step 2 review confirmed the deterministic compare engine should remain the authoritative fallback layer for severe-risk overrides, limited-data caution, tie conservatism, and source-confidence weighting.
- Step 3 QA pass showed the live compare experience is stable and safe, but the main weakness is repetitive narrative quality, with some token pairs also degrading into fallback-heavy resolution.
- Important strategic reminder from Ifeanyi: when Pete is ready and explicitly grants access, we plan to plug Pete's local machine compute API into GLADYS v2 as a future provider/inference path.
- This means GLADYS v2 planning should continue to preserve a provider-adapter architecture rather than assume only direct OpenAI API-key execution.
- Completed the first deterministic compare-intelligence refinement pass in:
  - `token-research-assistant/src/utils/generateGladysCompareInsight.ts`
  - `token-research-assistant/src/utils/generateGladysCompareInsight.js`
- The compare verdict layer now uses more scenario-aware wording for:
  - severe-risk loser cases,
  - cleaner-data-path winners,
  - close-call live winners with cleaner risk posture,
  - fallback-heavy ties,
  - and stronger metric-plus-risk alignment cases.
- Expanded regression coverage in `token-research-assistant/tests/gladysCompareInsight.test.mjs` to cover:
  - severe-risk safer-language,
  - mixed-data tie wording,
  - cleaner-risk close-call winner wording,
  - fallback-heavy tie explanation,
  - and preserved token-name rendering in reasons.
- Validation after this pass completed successfully:
  - `npm test` passed (18/18),
  - `npm run build` passed.
- Best next resume point:
  - manually inspect the updated compare copy in the browser on a few token pairs,
  - then inspect why some established pairs still degrade into fallback-heavy resolution,
  - and only after that decide whether to do a second deterministic polish pass or move into data-coverage fixes / UI polish.
- Committed and pushed the first deterministic compare-intelligence refinement checkpoint:
  - commit `6521914`
  - message: `Refine GLADYS compare verdict branching`
- Added a second refinement pass on `generateComparativeIntelligence` to improve summary differentiation for live-vs-live and mixed trade-off cases.
- Expanded `tests/comparativeIntelligence.test.mjs` with coverage for:
  - differentiated strong live-vs-live winner summaries,
  - mixed liquidity-vs-size trade-off wording.
- Validation after the second refinement pass completed successfully:
  - `npm test` passed (20/20).
- Recheck findings after the second pass:
  - comparative summaries are more product-like and specific,
  - but final GLADYS compare verdicts for strong live-vs-live pairs such as `bitcoin vs ethereum` and `bitcoin vs solana` still sound too similar at the `generateGladysCompareInsight` layer,
  - and upstream compare responses for some established pairs (notably `uniswap vs chainlink` and `bitcoin vs bitcoin-cash`) still inconsistently degrade into incomplete/fallback-heavy paths.
- Updated best next resume point:
  - do a targeted second-pass refinement inside `token-research-assistant/src/utils/generateGladysCompareInsight.ts` to better distinguish blue-chip-vs-blue-chip, blue-chip-vs-higher-beta, and credible-loser scenarios,
  - then separately investigate the upstream data/resolution inconsistency affecting established compare pairs.
- Completed the targeted second-pass refinement in `generateGladysCompareInsight` and updated classification logic for:
  - stronger large-cap setup phrasing,
  - credible large-cap loser vs higher-beta loser differentiation,
  - richer confidence and reasons tied to the loser profile.
- Expanded `tests/gladysCompareInsight.test.mjs` with coverage for:
  - credible large-cap loser wording,
  - higher-beta loser wording.
- Validation after this pass completed successfully:
  - `npm test` passed (22/22),
  - `npm run build` passed.
- Quick real-pair spot-check after the pass showed:
  - `bitcoin vs ethereum` and `bitcoin vs solana` now use stronger large-cap / aggressive-profile framing instead of the earlier flatter generic copy,
  - but Ethereum is still currently being classified into the more aggressive-profile branch alongside Solana, so blue-chip-vs-blue-chip nuance may still deserve another polish pass later.
- Best next resume point after this commit:
  - commit/push the GLADYS-layer refinement,
  - then later revisit whether Ethereum should stay in the same loser-profile bucket as Solana or whether the model needs a finer mid-tier/credible-major distinction.
