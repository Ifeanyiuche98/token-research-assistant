# 2026-05-10

- Resumed the Token Research Assistant compare-mode polish pass before OpenAI-backed GLADYS v2.
- Removed remaining raw Left/Right copy from comparative intelligence generation in:
  - `token-research-assistant/src/utils/generateComparativeIntelligence.ts`
  - `token-research-assistant/src/utils/generateComparativeIntelligence.js`
- Comparative intelligence summaries now use real token names in both:
  - per-card metric summaries,
  - the top comparative-intelligence summary sentence.
- Added direct regression coverage in `token-research-assistant/tests/comparativeIntelligence.test.mjs` to ensure:
  - token names replace Left/Right in comparative intelligence copy,
  - fallback/missing-data cases still use the present token name instead of raw side labels.
- Validation completed:
  - `npm test` passed (12/12),
  - `npm run build` passed.
- Browser validation results:
  - local dev build at `http://127.0.0.1:5173/` showed the updated compare copy correctly (`Bitcoin appears stronger...`, `Bitcoin shows stronger liquidity...`, `Bitcoin is larger...`),
  - deployed Vercel app at `https://token-research-assistant.vercel.app/` still showed stale comparative-intelligence copy (`Left appears stronger...`, `Left shows stronger liquidity...`, `Left is larger...`) while GLADYS card copy already used token names.
- Best current diagnosis:
  - local source and local runtime include the polish,
  - deployed Vercel output is still on an older bundle / deployment state for the comparative-intelligence section.
- Best next resume point:
  - commit the compare-copy polish,
  - push and/or trigger a fresh Vercel deployment,
  - re-test the deployed URL after the new deployment finishes.
- New direction to remember from Ifeanyi on 2026-05-10:
  - after this compare-polish work, we should also revisit the broader UI experience and visual design,
  - specifically plan for a more professional, top-tier interface with a stronger theme system, better font choices, and more readable color decisions.
- Started GLADYS v2 phase-1 implementation blueprint work.
- Added GLADYS v2 type scaffolding to the shared compare contract:
  - `gladysV2Insight` now exists on compare responses,
  - new v2 priority/confidence/output types were added to `src/types/compare.ts` and mirrored in `src/types/compare.d.ts`.
- Added compact v2 input builder scaffolding:
  - `src/utils/buildGladysV2Input.ts`
  - `src/utils/buildGladysV2Input.js`
- Added GLADYS v2 validation scaffold:
  - `src/utils/validateGladysV2Insight.ts`
  - `src/utils/validateGladysV2Insight.js`
  - current validation covers shape checks, confidence labels, grounding flag, limited-data overconfidence rejection, severe-risk recommendation rejection, and basic contradiction/hype checks.
- Added OpenAI-v2 generator scaffold:
  - `src/utils/generateGladysCompareInsightV2.ts`
  - `src/utils/generateGladysCompareInsightV2.js`
  - currently intentionally returns `null` because the real model call is not wired yet; this preserves deterministic fallback behavior while establishing prompt/build/validation structure.
- Wired compare response flow to carry `gladysV2Insight` safely in:
  - `src/lib/research/compare.ts`
  - `src/lib/research/compare.js`
- Added direct phase-1 regression coverage in `tests/gladysCompareInsightV2.test.mjs` for:
  - compact input building,
  - acceptance of grounded valid v2 output,
  - rejection of contradictory / hype-heavy invalid output.
- Validation after phase-1 scaffolding:
  - `npm test` passed (15/15),
  - `npm run build` passed.
- Best current resume point:
  - the v2 contract, input builder, validator, and safe null-return generator scaffold are in place,
  - next step is wiring the real OpenAI call into `generateGladysCompareInsightV2` and then rendering `gladysV2Insight` in the UI when valid.
- Continued into the next GLADYS v2 step.
- Wired a real OpenAI-backed GLADYS v2 request path in:
  - `src/utils/generateGladysCompareInsightV2.ts`
  - `src/utils/generateGladysCompareInsightV2.js`
- Current OpenAI wiring details:
  - uses `OPENAI_API_KEY` to enable the v2 call,
  - model resolution prefers `OPENAI_MODEL`, then `GLADYS_V2_MODEL`, then defaults to `gpt-4.1-mini`,
  - sends a disciplined system prompt + schema-constrained user prompt to `https://api.openai.com/v1/chat/completions`,
  - requests JSON object output, strips code fences if needed, parses JSON, validates it, and silently falls back to deterministic GLADYS on any failure.
- Updated the real production compare route in `api/compare.js` so deployed compare responses can now attach `gladysV2Insight` when the model call succeeds.
- Updated `src/components/GladysCompareCard.tsx` so the UI now prefers `gladysV2Insight` when present and valid, otherwise it continues rendering the deterministic v1 card.
- Validation after OpenAI wiring:
  - `npm test` passed,
  - `npm run build` passed.
- Important current state:
  - GLADYS v2 is now functionally wired,
  - but whether it actually appears at runtime depends on a valid `OPENAI_API_KEY` being configured in the deployment/runtime environment.
- Best next resume point:
  - verify whether the runtime/deployment has `OPENAI_API_KEY` available,
  - if yes, test live compare output and inspect whether GLADYS v2 renders successfully,
  - if not, add the env var and redeploy, then validate local + deployed behavior.
- Began refactoring GLADYS v2 away from direct API-key-only assumptions into a provider-adapter shape.
- `generateGladysCompareInsightV2` now has a provider concept in progress with intended modes:
  - `disabled`
  - `openai-api-key`
  - `remote-proxy`
- Current intended remote-proxy path design:
  - use `GLADYS_V2_PROVIDER=remote-proxy` or auto-detect via `GLADYS_V2_PROXY_URL`,
  - optional bearer auth via `GLADYS_V2_PROXY_TOKEN`,
  - send task name, model name, system prompt, user prompt, and structured payload to the proxy,
  - accept either `content` / `output` / `result` string fields or a structured `data` object from the proxy response.
- Interruption recovery note:
  - the TypeScript provider-adapter rewrite was completed,
  - the JS runtime file was then brought in line,
  - but the checkpoint validation revealed one remaining failing test before finalizing the refactor.
- Current checkpoint validation state:
  - `npm test` is NOT fully green yet,
  - failing test: `tests/compareApiContract.test.mjs`.
- Known modified files at checkpoint:
  - `token-research-assistant/src/utils/generateGladysCompareInsightV2.ts`
  - `token-research-assistant/src/utils/generateGladysCompareInsightV2.js`
  - `token-research-assistant/api/compare.js`
  - `token-research-assistant/src/components/GladysCompareCard.tsx`
  - `memory/2026-05-10.md`
- Best resume point now:
  - inspect and fix `tests/compareApiContract.test.mjs` against the provider-adapter refactor,
  - rerun `npm test`,
  - rerun `npm run build`,
  - then commit the provider-adapter checkpoint cleanly.
