# 2026-05-09

- Resumed Token Research Assistant Phase 3 from the saved GLADYS compare verdict stopping point.
- Reviewed the compare response path and confirmed the cleaner architecture is to move GLADYS compare verdict generation into the shared compare response contract instead of leaving it only in the UI layer.
- Promoted compare GLADYS into shared contract flow:
  - added `GladysCompareInsight` to `src/types/compare.ts`,
  - added `gladysInsight` to `CompareResponse`,
  - updated `src/lib/research/compare.ts` to generate `comparativeIntelligence` first and then generate `gladysInsight` on the shared comparison object,
  - updated `GladysCompareCard` to consume `comparison.gladysInsight` instead of recomputing verdicts client-side.
- Validation after shared-contract promotion:
  - `npm run build` passed,
  - `npm test` passed.
- New practical resume point:
  - compare verdict logic now lives in the shared compare response contract,
  - next step should be either stronger compare heuristics on the shared contract path or OpenAI-backed GLADYS v2 using this contract as the deterministic fallback/base.
- Started the next step: strengthening compare heuristics on the shared contract path before any OpenAI-backed GLADYS v2 work.
- Strengthened shared compare heuristics in `src/utils/generateGladysCompareInsight.ts`.
- Heuristic improvements now include:
  - weighted scoring instead of simple win-counting,
  - stronger risk influence, including explicit severe-risk penalties,
  - source-confidence influence,
  - fallback / limited-data penalties,
  - tie handling that prefers caution when the edge is too small,
  - improved reasons for limited-data and severe-risk cases.
- Validation after heuristic strengthening:
  - `npm run build` passed,
  - `npm test` passed.
- New best resume point:
  - deterministic compare verdict logic now lives in the shared contract and uses stronger weighting,
  - next logical step is either dedicated compare-verdict tests, another UI/behavior validation pass, or OpenAI-backed GLADYS v2 built on top of this stronger fallback.
- Started the next step: adding dedicated compare-verdict tests for the stronger shared GLADYS compare heuristics.
- Added dedicated compare-verdict tests in `tests/gladysCompareInsight.test.mjs`.
- Covered behaviors now include:
  - severe-risk cases where cleaner risk posture should beat simple metric wins,
  - mixed / limited-data cases where GLADYS should fall back to tie-style language instead of overstating a winner,
  - reason-copy checks to ensure token names replace Left/Right labels.
- During test integration, discovered the JS companion file used by the Node test runner had drifted behind the TypeScript source, so `src/utils/generateGladysCompareInsight.js` was synced to the current logic.
- Validation after adding dedicated compare-verdict tests:
  - `npm test` passed (9/9),
  - `npm run build` passed.
- New best resume point:
  - shared deterministic compare verdict logic is now both strengthened and directly covered by dedicated tests,
  - next logical step is UI/behavior validation of the new edge cases or moving into OpenAI-backed GLADYS v2 with this tested deterministic fallback.
- Started UI/behavior validation pass for the new compare edge cases.
- UI/behavior validation completed against two browser-stubbed compare scenarios:
  - severe-risk override case (`RiskyDex` vs `SteadyChain`),
  - mixed-data small-edge tie case (`TokenAlpha` vs `TokenBeta`).
- Validation result for severe-risk override:
  - GLADYS correctly favored the structurally cleaner asset even when the risky asset led on raw size/liquidity,
  - the card explicitly surfaced severe caution, limited-data weakness, and cleaner risk posture,
  - visually and behaviorally this looked correct.
- Validation result for mixed-data tie case:
  - GLADYS correctly fell back to “No clean winner yet”,
  - used trade-off language instead of bluffing a winner,
  - confidence copy and supporting reasons matched the intended cautious behavior.
- One minor UX observation remains:
  - the lower “Comparative intelligence” section still uses Left/Right wording in its internal cards, even though GLADYS reasons now use token names.
  - This is not a GLADYS bug, but it is a polish opportunity if we want naming consistency across compare mode.
- Current best resume point after validation:
  - deterministic compare verdict logic is shared, strengthened, tested, and visually validated on key edge cases,
  - next logical step is OpenAI-backed GLADYS v2 or a smaller compare-mode polish pass for non-GLADYS Left/Right wording.
